# cy-basic_v2

Sample project for the basic course of the Talking About Testing (TAT) online school.

## Pre-requirements

It is required to have Node.js and npm installed to run this project.

> I used versions `v20.16.0` and `10.8.1` of Node.js and npm, respectively. I suggest you use the same or later versions.

## Installation

Run `npm install` (or `npm i` for the short version) to install the dev dependencies.

## Tests

Run `npm test` (or `npm t` for the short version) to run the test in headless mode.

Or, run `npm run cy:open` to open Cypress in interactive mode.

For tests with mobile viewport, add `:mobile` at the endo of the script.

## Support this project

If you want to support this project, leave a ⭐.

A special thanks to [Walmyr](https://walmyr.dev) for his project `Talking About Testing`, teaching about Cypress.

___

This project was created by [Gustavo Pasquini](https://github.com/Gustavo-Pasquini).